Q => How to fix committing to the wrong Git branch?
1. If you haven't yet pushed your changes, you can also do a soft reset:
      git reset --soft HEAD^
2.    git checkout branch
      git commit -c ORIG_HEAD
The -c ORIG_HEAD part is useful to not type commit message again.      
